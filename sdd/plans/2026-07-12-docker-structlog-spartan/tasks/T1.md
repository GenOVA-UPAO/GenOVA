# T1 — Docker frontend + compose.prod

**Model**: Composer
**State**: done

## Decisions made

- Quitado `pnpm install` del command de compose (deps de imagen + volumen anónimo).
- BuildKit cache mount para store pnpm.
- `Dockerfile.prod` multi-stage → nginx SPA.
- `docker-compose.prod.yml` nuevo (antes referenciado pero ausente).

## Files touched

- `frontend/Dockerfile`, `frontend/Dockerfile.prod`, `frontend/nginx.conf`
- `docker-compose.yml`, `docker-compose.prod.yml`

## Final receipt

`done -> Docker dev sin reinstall en up; prod multi-stage + compose.prod creados`

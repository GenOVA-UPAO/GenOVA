# T2 — structlog + redaction + main≤200

**Model**: Grok
**State**: done

## Decisions made

- ProcessorFormatter + JSON en prod / Console en dev.
- `RedactingFilter` en handlers + `redact_event_dict` processor.
- Middlewares en `core/http_middleware.py`; request_id en `RequestContextMiddleware`.

## Files touched

- `backend/core/logging_setup.py`, `http_middleware.py`, `log_redaction.py`
- `backend/main.py`, deps, `tests/test_logging_setup.py`

## Verification evidence

```
12 passed (test_log_redaction + test_logging_setup + test_langsmith_init)
verify.ps1 -Quick → RESULTADO FINAL: PASA
```

## Final receipt

`done -> structlog configurado; main.py ≤200; R8 dual; tests verdes`

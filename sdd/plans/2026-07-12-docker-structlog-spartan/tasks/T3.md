# T3 — LangSmith opt-in

**Model**: Grok
**State**: done

## Decisions made

- Opt-in: `langsmith_api_key` + `langsmith_tracing`; sin key = no-op.
- `init_langsmith()` setea env vars oficiales antes de invokes.
- `build_invoke_config` añade tags/metadata sin PII.

## Files touched

- `backend/core/config.py`, `observability.py`, `logging_setup.py`
- `backend/prometheus/engine/graph.py`
- `backend/tests/test_langsmith_init.py`, `.env.example`, deps

## Final receipt

`done -> LangGraph↔LangSmith opt-in con tests no-op/enabled`

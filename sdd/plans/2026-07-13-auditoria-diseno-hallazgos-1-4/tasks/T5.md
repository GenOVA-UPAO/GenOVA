# T5 — Layout moderado (shell 7xl, páginas, grid 4-col, crear-OVA)

Estado: implementado y verificado.

## Archivos tocados (antes → después)

1. `frontend/src/app/layout/components/main-container.component.ts` (línea 15)
   - `class="mx-auto w-full max-w-6xl p-4 ..."` → `max-w-7xl` (1280px). `mx-auto` intacto.

2. `frontend/src/features/ova-library/pages/dashboard-page.html` (línea 2)
   - `class="mx-auto max-w-5xl space-y-10 ..."` → `max-w-7xl`.

3. `frontend/src/features/llm-settings/pages/models-page.component.html`
   - Línea 1: `class="mx-auto max-w-5xl space-y-6 pb-28 p-8 ..."` → `max-w-7xl`.
   - Línea ~112 (barra sticky de cambios sin guardar): `class="mx-auto flex max-w-5xl items-center justify-between gap-3"` → `max-w-7xl`, para que la barra quede alineada con el contenido de arriba.

4. `frontend/src/features/ova-library/pages/mis-ovas-page.html`
   - Línea 1: ya estaba en `max-w-7xl` — sin cambios (el shell era la restricción real, confirmado).
   - Línea ~188 grid: `class="grid gap-5 sm:grid-cols-2 xl:grid-cols-3"` → `class="grid items-stretch gap-5 sm:grid-cols-2 xl:grid-cols-3 2xl:grid-cols-4"` (añade paso 4-col en `2xl` y `items-stretch` para igualar alturas de celda).
   - Línea ~190 wrapper de card: `class="animate-in zoom-in-95 duration-300"` → `class="h-full animate-in zoom-in-95 duration-300"`.

5. `frontend/src/features/ova-library/components/cards/ova-card-shell.component.ts`
   - Raíz del card (`defaultRootClass`, línea ~64): `rounded-xl border bg-card p-5 shadow-sm hover:shadow-md transition ...` → `flex h-full flex-col rounded-xl border bg-card p-5 shadow-sm hover:shadow-md transition ...`.
   - Bloque de contenido (línea ~14, `<div class="flex items-start gap-3">`) → `<div class="flex flex-1 items-start gap-3">`. Con la raíz en `flex flex-col h-full` y este bloque en `flex-1`, el bloque de footer (`<ng-content select="[footer]">`, con `border-t` y `mt-4`) queda empujado al fondo del card sin importar el largo de título/descripción/owner/fecha. Verificado visualmente en el template: el footer es el segundo (y último) hijo directo de la raíz, así que no hace falta tocarlo — al crecer el primer hijo, el segundo se ancla abajo.

6. `frontend/src/features/ova-workspace/components/creation/ova-create-form-card.component.html` (línea 14)
   - `class="my-auto w-full max-w-2xl space-y-5"` → `class="mt-8 w-full max-w-4xl space-y-5"`.
   - Ancho: se usó `max-w-4xl` (896px) en vez de `max-w-3xl` (768px) porque el propio plan (`2026-07-13-auditoria-diseno-hallazgos-1-4.md` §Item 2) apunta a "~900px", y la instrucción del task explícitamente habilitaba esa alternativa por criterio propio. Sigue siendo claramente un formulario centrado, no full-bleed.
   - Centrado vertical: `my-auto` (centrado absoluto, dejaba mucho hueco arriba/abajo en viewports 900–1080px) → `mt-8` (arranca cerca del top con margen fijo, sin over-engineering).

## Decisión sin cambio — dashboard stats/actividad (`sm:grid-cols-3`, líneas ~50 y ~185)

Revisadas ambas grillas (`stats()` con 3 `gn-dashboard-stat-card`, y el panel admin con `adminCards`)
con el contenedor ya en `max-w-7xl`. A 1280px, 3 columnas siguen dando ~400px por celda, un ancho
razonable para stat cards y cards de admin (no quedan ni angostas ni excesivamente estiradas). Se
dejó **sin cambios** — no se detectó necesidad de un paso `xl`/`2xl` adicional; forzar 4+ columnas
ahí produciría cards más chatas sin beneficio de lectura.

## Verificación

```
cd frontend && pnpm lint         # OK, sin errores
cd frontend && pnpm typecheck    # OK (ngc app + spec), sin errores
cd frontend && pnpm --filter frontend test   # OK: 11 test files / 45 tests pasan
```

`ova-create-form-card.component.spec.ts` (único spec existente entre los componentes tocados) sigue
en verde tras el cambio de `max-w-2xl`→`max-w-4xl` y `my-auto`→`mt-8`. No existen specs para
`ova-card-shell.component.ts` ni `mis-ovas-page.html`/`dashboard-page.html` — no se crearon nuevos
por ser fuera del alcance de T5 (cambios puramente de clases Tailwind, sin lógica).

## Nota — cambios concurrentes de otros subagentes

Durante la ejecución de T5, `dashboard-page.html` fue tocado en paralelo por otro subagente
(fix T7, NG0956 track-by-identity: `track stat` → `track stat.label`, `track ova` → `track ova.id`,
`track card` → `track card.title`). No hubo conflicto: mi único cambio en ese archivo
(`max-w-5xl` → `max-w-7xl`, línea 2) convive sin problema con los `track` actualizados.

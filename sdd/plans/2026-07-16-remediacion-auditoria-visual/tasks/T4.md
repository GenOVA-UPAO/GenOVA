# T4 — Modales /crear coherentes (CR-02, media)

Nota: el subagente original murió por límite de sesión con el grueso aplicado;
el hilo principal validó el diff, arregló 1 error de prettier y completa esta evidencia.

## Cambios

- `frontend/src/core/services/modal-stack.service.ts` (nuevo) + spec — stack
  compartido: solo el modal superior consume Escape.
- `frontend/src/core/directives/modal-dismiss.directive.ts` — se registra en el
  stack; `Escape` solo dispara si `isTop()`. Con el config anidado del picker 5E
  abierto, Escape cierra solo el anidado.
- `frontend/src/core/components/ui/dialog.component.ts` — gn-dialog integrado
  con el stack (Esc/backdrop → openChange consistente).
- `frontend/src/features/ova-workspace/components/modals/ova-theme-modal.component.ts`
  — reescrito sobre `gn-dialog` (antes overlay a mano SIN Escape); dismissal
  por Esc/backdrop/X siempre cierra de verdad.
- `frontend/src/features/ova-workspace/components/creation/ova-create-form-card.component.{ts,html}`
  — `showFiles`/`showTheme` de booleanos planos a **signals** (fix del modal
  "fantasma" que reaparecía en zoneless/OnPush); `openFiles()`/`openTheme()`
  con exclusión mutua + output `closePicker` cableado en
  `ova-creation-view.component.ts` a `flow.closeModal()` (cierra el picker 5E).

## Verificación

- verify.ps1 -Quick PASA (lint + typecheck + 88 unit tests, incluye spec nuevo del stack).
- Runtime (Escape single-close, no reaparición, no stacking) pendiente del hilo principal.

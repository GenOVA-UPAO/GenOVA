# T2 — Responsive /mis-ovas y cards (MO-01, ALTA)

Nota: el subagente original murió por límite de sesión con el trabajo ya aplicado;
el hilo principal validó el diff y completa esta evidencia.

## Cambios

- `frontend/src/features/ova-library/pages/mis-ovas-page.html` — grid con
  `grid-cols-1` base (antes solo `sm:grid-cols-2 xl:grid-cols-3 2xl:grid-cols-4`,
  que bajo 640px dejaba una columna implícita max-content de ~619px).
- `frontend/src/features/ova-library/pages/papelera-page.html` — mismo fix.
- `frontend/src/features/ova-library/components/cards/ova-card.component.ts` —
  filas de acciones pasan de `flex` a `grid grid-cols-2` con `w-full min-w-0
  truncate` por botón ("Papelera" ocupa `col-span-2`); ya no fuerzan el ancho
  max-content del card. Botón papelera con `aria-label="Enviar a papelera"`.

## Verificación

- `pnpm --filter frontend lint` + `typecheck` + `test` → verdes (verify.ps1 -Quick PASA).
- Visual 375px pendiente del hilo principal.

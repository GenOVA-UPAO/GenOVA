version: 2
updates:
  # Mantiene las acciones de los workflows en su última versión (abre PRs).
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    commit-message:
      prefix: "ci"
    groups:
      actions:
        patterns: ["*"]

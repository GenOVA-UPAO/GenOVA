# Core infrastructure — strictly pinned for reproducibility (latest at 2026-06-19)
fastapi==0.141.1
uvicorn[standard]==0.52.1
sqlalchemy==2.0.52
psycopg[binary]==3.3.4
python-dotenv==1.2.2
bcrypt==5.0.0
PyJWT==2.13.0
python-multipart==0.0.32
pydantic[email]==2.13.4
pydantic-settings==2.15.0
slowapi==0.1.10
sse-starlette==3.4.8
# Job queue + cache (B2/B3) — arq pulls redis-py, also used by slowapi storage
arq==0.28.0
# Observability
sentry-sdk[fastapi]==2.67.1
prometheus-fastapi-instrumentator==8.1.0
# NO agregar el extra [fastapi]: opentelemetry-instrumentation-fastapi 0.63b1 es
# incompatible con FastAPI 0.138 (routing con _IncludedRouter sin `.path`) y crashea
# el middleware ASGI en cada request /api/* (500 antes de CORS → login roto). Ver
# core/observability.py::init_logfire.
logfire[sqlalchemy]==4.40.0
structlog==26.1.0
# Multi-agent orchestration (Prometheus via LangGraph) + LangSmith traces
langgraph==1.2.11
langgraph-checkpoint-postgres==3.1.2
langsmith==0.10.18
# AI SDKs
openai==2.43.0
groq==1.6.0
# Structured LLM output (Pydantic-validated JSON + auto-retry) — wraps openai/groq
instructor==1.15.4
supabase==2.31.0
google-genai==2.18.0
pypdf==6.15.0
python-docx==1.2.0
python-pptx==1.0.2
filetype==1.2.0
# 2FA
pyotp==2.10.0

"""Dominio de autenticación (arquitectura hexagonal).

- Guards HTTP: `auth.dependencies` (compat) / `auth.interface.http.dependencies`.
- Router: `auth.interface.http.router`.
- Normalización de correo (dominio): `auth.domain.email`.
"""
